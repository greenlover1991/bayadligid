<h1>Editing loan for <%= employee_full_name(params[:employee_id]) %></h1>

<%= render 'form' %>

<div class="extra">
	<%= link_to 'Show', [@applied_loan.employee, @applied_loan] %> |
	<%= link_to 'Back', employee_path(params[:employee_id]) %>
</div>
